#include<conio.h>
long countprime(long a,long k)
{
long i;
for(i=a;i>=2;i--)
{
return 1;
}
void main()
{
long i,range=100;
long count=1;
double x;

for(i=3;i<range;i++)
{
x=sqrt(i);
cou+=couprime(floor(x),i);
}
printf("\n Total number of prime numbers are %ld",count);
getch();
}
